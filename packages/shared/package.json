{
  "name": "@opencode-flow-engine/shared",
  "version": "1.0.0",
  "description": "Shared utilities for opencode-flow-engine",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --outDir dist --declaration --declarationMap && bun build --target=node --outdir=dist --minify src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "license": "MIT"
}
